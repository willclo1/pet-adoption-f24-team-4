package petadoption.api.pet.criteria.breed;

public interface AnimalBreed {
    String getAnimalType();
    //String getBreedName();
    //String getAnimalType();
}

/*
public class AnimalTest {
    public static void main(String[] args) {
        AnimalBreed dogBreed = DogBreed.LABRADOR;
        AnimalBreed catBreed = CatBreed.PERSIAN;

        System.out.println(dogBreed.getBreedName() + " is a " + dogBreed.getAnimalType());
        System.out.println(catBreed.getBreedName() + " is a " + catBreed.getAnimalType());
    }
}
*/